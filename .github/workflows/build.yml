on:
 pull_request:
    types: [opened, reopened]
    branches:
      - master   
 workflow_dispatch:
 push:
    branches:    
      - 'master'
      - 'rc'
    paths:
      - '.github/**'
      - 'config/**'
      - 'build.yaml'

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
